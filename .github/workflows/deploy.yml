name: Deploy

# Деплой на VPS после каждого пуша в main (после успешного CI).
# Требует секретов в Settings → Secrets and variables → Actions:
#   SSH_HOST — IP сервера
#   SSH_USER — пользователь (operator)
#   SSH_KEY  — приватный SSH-ключ (содержимое файла)

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success'
    # Пока секреты не заданы — job тихо пропускается, ничего не ломая.
    steps:
      - name: Check secrets
        id: check
        run: |
          if [ -n "${{ secrets.SSH_HOST }}" ]; then
            echo "ok=true" >> "$GITHUB_OUTPUT"
          else
            echo "SSH-секреты не настроены — деплой пропущен."
          fi

      - name: Deploy over SSH
        if: steps.check.outputs.ok == 'true'
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            set -e
            cd ~/gamecore
            git pull --ff-only
            ~/.local/bin/uv sync --frozen
            ~/.local/bin/uv run alembic upgrade head
            systemctl --user restart gamecore
            systemctl --user --no-pager status gamecore
